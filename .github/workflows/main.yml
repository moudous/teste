on:
  push:
    branches:
      - main

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar repositório
        uses: actions/checkout@v4

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ftp.nossafco.com.br
          username: teste
          password: BleDK102dLavyZMAfqRZ
          port: 21
          protocol: ftp
          local-dir: ./
          server-dir: ./
          dangerous-clean-slate: false